# MCO — Pinned dependencies
# All versions must be pinned. Tokenizer drift and sklearn changes
# are the two most common sources of silent measurement corruption.

# Core ML
torch==2.2.0
transformers==4.38.2
tokenizers==0.15.2        # PIN THIS — tokenizer behavior changes across versions
sentence-transformers==2.6.1

# Numerics
numpy==1.26.4
scipy==1.12.0
scikit-learn==1.4.1.post1

# NLP utilities
datasets==2.18.0           # HuggingFace datasets (for Phase 5 TinyStories)


# Dashboard
streamlit==1.32.2          # Phase 3 deliverable

# Experiment tracking
wandb==0.16.4              # W&B (Kaggle sessions only)

# SQLite interface (stdlib, no pin needed)
# better-sqlite3 is used by the MCP server (separate package.json)

# Dev tools
pytest==8.1.0
